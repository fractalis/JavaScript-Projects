function handleClick() {
    var gameContainer = document.getElementById('game-container');

    gameContainer.style['backgroundColor'] = '#afefef';
    gameContainer.innerHTML = '<p>Game Starting</p>';
}